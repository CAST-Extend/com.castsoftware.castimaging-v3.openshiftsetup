apiVersion: v1
kind: Service
metadata:
  labels:
    app: castimaging
    imaging.group: console
    imaging.service: console-service
  name: console-service
  namespace: {{ .Release.Namespace }}  
spec:
  ports:
    - name: "8091"
      port: 8091
      targetPort: 8091
  selector:
    imaging.service: console-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: castimaging
    imaging.group: console
    imaging.service: console-service
    version: {{ .Values.ConsoleServiceImage.tag| default .Values.version }}
  name: console-service
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.ConsoleServiceReplicaCount }}
  selector:
    matchLabels:
      imaging.service: console-service
  strategy: {}
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        console.network/aip-network: "true"
        app: castimaging
        imaging.group: console
        imaging.service: console-service
    spec:
{{- if .Values.EnforceNodeAffinity }}
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.nodeSelectorAppcastimaging | nindent 12 }}
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorServiceSso | nindent 12 }}
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupConsole | nindent 12 }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorGroupViewer | nindent 12 }}
{{- end }}
{{- if .Values.EnforceBalancedAffinity }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupViewer | nindent 12 }}
{{- end }}
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      initContainers:
      - name: console-control-panel-healthcheck
        image: {{ .Values.InitUtilImage.name }}:{{ .Values.InitUtilImage.tag }}
        imagePullPolicy: "{{ .Values.InitUtilImage.pullPolicy }}"
        command:
        - /bin/sh
        - -c
        - |
          until curl -s --fail --max-time 5 http://console-control-panel:8098/actuator/health | grep -q '"status":"UP"'; do
            echo "Waiting for console-control-panel to be healthy..."
            sleep 2
          done
          echo "console-control-panel is healthy!"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        resources:
          {{- toYaml .Values.InitContainerResources | nindent 12 }}
      - name: postgres-scripts
        image: {{ .Values.InitUtilImage.name }}:{{ .Values.InitUtilImage.tag }}
        imagePullPolicy: "{{ .Values.InitUtilImage.pullPolicy }}"
        env:
          - name: PGPASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.existingSecret | default "imaging-pwd-sec" }}
                key: operator-db-password
        command:
        - /bin/sh
        - -c
        - |
          echo "Running context URL updates..."
{{- if .Values.CustomPostgres.enable }}
          psql -h {{ .Values.CustomPostgres.host }} -p {{ .Values.CustomPostgres.port }} -U operator -d postgres -f /home/imaging/sql/contexturl-controlpanel-update-script.sql
          psql -h {{ .Values.CustomPostgres.host }} -p {{ .Values.CustomPostgres.port }} -U operator -d keycloak -f /home/imaging/sql/contexturl-keycloak-update-script.sql
          psql -h {{ .Values.CustomPostgres.host }} -p {{ .Values.CustomPostgres.port }} -U operator -d postgres -f /home/imaging/sql/license-extend-update-script.sql
{{- else }}
          psql -h console-postgres -p 2285 -U operator -d postgres -f /home/imaging/sql/contexturl-controlpanel-update-script.sql
          psql -h console-postgres -p 2285 -U operator -d keycloak -f /home/imaging/sql/contexturl-keycloak-update-script.sql
          psql -h console-postgres -p 2285 -U operator -d postgres -f /home/imaging/sql/license-extend-update-script.sql
{{- end }}
          echo "Done."
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        resources:
          {{- toYaml .Values.InitContainerResources | nindent 12 }}
        volumeMounts:
          - name: contexturl-controlpanel-update-script
            mountPath: /home/imaging/sql/contexturl-controlpanel-update-script.sql
            subPath: contexturl-controlpanel-update-script.sql
          - name: contexturl-keycloak-update-script
            mountPath: /home/imaging/sql/contexturl-keycloak-update-script.sql
            subPath: contexturl-keycloak-update-script.sql
          - name: license-extend-update-script
            mountPath: /home/imaging/sql/license-extend-update-script.sql
            subPath: license-extend-update-script.sql
      containers:
        - env:
{{- range $key, $value := .Values.CONSOLESERVICE }}
            - name: {{ $key }}
              value: {{ $value | quote }}
{{- end }}
            - name: HOST_CONSOLE
              value: console-service
            - name: PORT_CONSOLE
              value: "8091"
            - name: HOST_CONTROL_PANEL
              value: console-control-panel
            - name: PORT_CONTROL_PANEL
              value: "8098"
            - name: HOST_AUTH_SERVICE
              value: console-authentication-service
            - name: PORT_AUTH_SERVICE
              value: "8092"
          image: "{{ .Values.ConsoleServiceImage.name }}:{{ .Values.ConsoleServiceImage.tag}}"
          imagePullPolicy: "{{ .Values.ConsoleServiceImage.pullPolicy }}"
          name: console-service
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8091
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 20
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8091
              scheme: HTTP
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8091
              scheme: HTTP
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          ports:
            - containerPort: 8091
              protocol: TCP
          resources:
            {{- toYaml .Values.ConsoleServiceResources | nindent 12 }} 
          volumeMounts:
            - mountPath: {{ .Values.anadir.datadirmount }}
              name: shared-datadir
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: shared-datadir
          persistentVolumeClaim:
            claimName: pvc-shared-datadir
        - name: contexturl-controlpanel-update-script
          configMap:
            name: contexturl-controlpanel-update-script
        - name: contexturl-keycloak-update-script
          configMap:
            name: contexturl-keycloak-update-script
        - name: license-extend-update-script
          configMap:
            name: license-extend-update-script
      restartPolicy: Always
{{- if .Values.UseImagePullSecrets }}       
      imagePullSecrets:
        - name: imagepull-sec  
{{- end }}