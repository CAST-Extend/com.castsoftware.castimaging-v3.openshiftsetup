kind: Service
apiVersion: v1
metadata:
  name: viewer-aimanager
  labels:
    app: castimaging
    imaging.group: viewer
    imaging.service: viewer-aimanager
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: http
    port: 8082
    targetPort: 8082
  - name: graphrag
    port: 8286
    targetPort: 8286
  - name: agents
    port: 8030
    targetPort: 8030
  selector:
    imaging.service: viewer-aimanager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: castimaging
    imaging.group: viewer
    imaging.service: viewer-aimanager
    version: {{.Values.ViewerAiManagerImage.tag| default .Values.version}}
  name: viewer-aimanager
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.ViewerAiManagerReplicaCount }}
  selector:
    matchLabels:
      imaging.service: viewer-aimanager
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app: castimaging
        imaging.group: viewer
        imaging.service: viewer-aimanager
    spec:
{{- if .Values.EnforceNodeAffinity }}
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.nodeSelectorAppcastimaging | nindent 12 }}
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorServiceNeo4j | nindent 12 }}
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupViewer | nindent 12 }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorGroupConsole | nindent 12 }}
{{- end }}
{{- if .Values.EnforceBalancedAffinity }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupConsole | nindent 12 }}
{{- end }}
      initContainers:
        - name: neo4j-and-control-panel-healthcheck
          image: {{ .Values.InitUtilImage.name }}:{{ .Values.InitUtilImage.tag }}
          imagePullPolicy: "{{ .Values.InitUtilImage.pullPolicy }}"
          command:
          - /bin/sh
          - -c
          - |
            until curl -s --fail --max-time 5 http://console-control-panel:8098/actuator/health | grep -q '"status":"UP"'; do
              echo "Waiting for console-control-panel to be healthy..."
              sleep 2
            done
            echo "console-control-panel is healthy!"  
            until curl -s --fail --max-time 5 http://viewer-neo4j:7474 > /dev/null 2>&1; do 
              echo "Waiting for neo4j..."
              sleep 2
            done
            echo "viewer-neo4j is up!"
        - name: init-folders
          image: "{{.Values.ViewerAiManagerImage.name}}:{{.Values.ViewerAiManagerImage.tag}}"
          imagePullPolicy: "{{.Values.ViewerAiManagerImage.pullPolicy}}"
          command:
          - /bin/sh
          - -c
          - |
            rm -rf /open-ai-volume/*
            cp -r /opt/imaging/open_ai-manager/* /open-ai-volume/ || true
          volumeMounts:
            - name: open-ai-dir
              mountPath: /open-ai-volume
      containers:
        - env:
{{- range $key, $value := .Values.AIMANAGER }}
            - name: {{ $key }}
              value: {{ $value | quote }}
{{- end }}
            - name: TZ
              value: "UTC"
            - name: NEO4J_BOLT_ENDPOINT
              value: "bolt://viewer-neo4j:7687"
            - name: LOG_LEVEL
              value: "debug"
            - name: EUREKA_HOST
              value: "console-control-panel"
            - name: EUREKA_PORT
              value: "8098"
            - name: SERVICE_HOST
              value: "viewer-aimanager"
            - name: AIMANAGER_PORT
              value: "8082"
            - name: ETL_PORT
              value: "9001"
            - name: NEO4J_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default "imaging-pwd-sec" }}
                  key: neo4j-password
{{- if .Values.ContextUrl.enable }}
            - name: APP_CONTEXT
              value: {{ .Values.ContextUrl.appcontext }}
{{- end }}
            # Variables for IMAGING-GRAPH-RAG
            - name: EMBEDDING_SELECTION
              value: "source_code,business_rule_extraction"
            - name: AI_EMBEDDING_MODEL
              value: "text-embedding-3-small"
          image: "{{ .Values.ViewerAiManagerImage.name }}:{{ .Values.ViewerAiManagerImage.tag}}"
          imagePullPolicy: "{{ .Values.ViewerAiManagerImage.pullPolicy }}"
          command: ['sh', '-c', "/opt/imaging/open_ai-manager/config/init.sh"]
          name: viewer-aimanager
          ports:
            - containerPort: 8082
              protocol: TCP
            - containerPort: 8286
              protocol: TCP
            - containerPort: 8030
              protocol: TCP
          startupProbe:
            tcpSocket:
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 30
          livenessProbe:
            tcpSocket:
              port: 8082
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 30
            successThreshold: 1
          resources:
            {{- toYaml .Values.ViewerAiManagerResources | nindent 12 }}
          volumeMounts:
            - name: open-ai-dir
              mountPath: /opt/imaging/open_ai-manager
            - name: logdir
              mountPath: /opt/imaging/open_ai-manager/logs
              readOnly: false
{{- if .Values.UseImagePullSecrets }}               
      imagePullSecrets:
        - name: imagepull-sec
{{- end }}        
      volumes:
        - name: logdir
          persistentVolumeClaim:
            claimName: pvc-imagingviewer-v3-aimanager-logs
        - name: open-ai-dir
          emptyDir: {}