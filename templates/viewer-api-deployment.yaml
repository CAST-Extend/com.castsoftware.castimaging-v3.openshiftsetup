kind: Service
apiVersion: v1
metadata:
  name: viewer-api
  labels:
    app: castimaging
    imaging.group: viewer
    imaging.service: viewer-api
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: http
    port: 8070
    targetPort: 8070
  selector:
    imaging.service: viewer-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: castimaging
    imaging.group: viewer
    imaging.service: viewer-api
    version: {{.Values.ViewerApiImage.tag| default .Values.version}}
  name: viewer-api
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.ViewerApiReplicaCount }}
  selector:
    matchLabels:
      imaging.service: viewer-api
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        app: castimaging
        imaging.group: viewer
        imaging.service: viewer-api
    spec:
{{- if .Values.EnforceNodeAffinity }}
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.nodeSelectorAppcastimaging | nindent 12 }}
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorServiceNeo4j | nindent 12 }}
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupViewer | nindent 12 }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorGroupConsole | nindent 12 }}
{{- end }}
{{- if .Values.EnforceBalancedAffinity }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupConsole | nindent 12 }}
{{- end }}
      initContainers:
        - name: neo4j-and-control-panel-healthcheck
          image: {{ .Values.InitUtilImage.name }}:{{ .Values.InitUtilImage.tag }}
          imagePullPolicy: "{{ .Values.InitUtilImage.pullPolicy }}"
          command:
          - /bin/sh
          - -c
          - |
            until curl -s --fail --max-time 5 http://console-control-panel:8098/actuator/health | grep -q '"status":"UP"'; do
              echo "Waiting for console-control-panel to be healthy..."
              sleep 2
            done
            echo "console-control-panel is healthy!"  
            until curl -s --fail --max-time 5 http://viewer-neo4j:7474 > /dev/null 2>&1; do 
              echo "Waiting for neo4j..."
              sleep 2
            done
            echo "viewer-neo4j is up!"
          volumeMounts:
            - name: logdir
              mountPath: /opt/imaging/imaging-api/logs
              readOnly: false
        - name: init-folders
          image: "{{ .Values.ViewerApiImage.name }}:{{ .Values.ViewerApiImage.tag }}"
          imagePullPolicy: "{{.Values.ViewerApiImage.pullPolicy}}"
          command:
          - /bin/sh
          - -c
          - |
            rm -rf /api-config-volume/*
            cp -r /opt/imaging/imaging-api/config/* /api-config-volume/ || true
          volumeMounts:    
            - name: api-config-dir
              mountPath: /api-config-volume 
      containers:
        - env:
{{- range $key, $value := .Values.API }}
            - name: {{ $key }}
              value: {{ $value | quote }}
{{- end }}
            - name: TZ
              value: "UTC"
            - name: LOG_LEVEL
              value: "debug"
            - name: EUREKA_HOST
              value: "console-control-panel"
            - name: EUREKA_PORT
              value: "8098"
            - name: SERVICE_HOST
              value: "viewer-api"
            - name: IMAGING_APIS_PORT
              value: "8070"
            - name: NEO4J_BOLT_ENDPOINT
              value: "bolt://viewer-neo4j:7687"
            - name: API_KEY
              value: 
            - name: SOURCE_CODE_SERVICE_TOKEN
              value: 
            - name: SOURCE_CODE_SERVICE_ENDPOINT
              value: 
            - name: TRUSTED_PROXIES
              value: 
            - name: ACCESS_CONTROL_ALLOW_ORIGIN
              value: 
            - name: ACCESS_CONTROL_ALLOW_METHODS
              value: 
            - name: ACCESS_CONTROL_ALLOW_HEADERS
              value: 
            - name: QUERY_TIMEOUT
              value: "60"
            - name: NEO4J_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default "imaging-pwd-sec" }}
                  key: neo4j-password
{{- if .Values.ContextUrl.enable }}
            - name: APP_CONTEXT
              value: {{ .Values.ContextUrl.appcontext }}
{{- end }}
          image: "{{ .Values.ViewerApiImage.name }}:{{ .Values.ViewerApiImage.tag }}"
          imagePullPolicy: "{{ .Values.ViewerApiImage.pullPolicy }}"
          command: ['sh', '-c', "/opt/imaging/imaging-api/config/init.sh"]
          name: viewer-api
          ports:
            - containerPort: 8070
              protocol: TCP
          startupProbe:
            tcpSocket:
              port: 8070
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 30
          livenessProbe:
            tcpSocket:
              port: 8070
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 30
            successThreshold: 1
          readinessProbe:
            tcpSocket:
              port: 8070
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 30
            successThreshold: 1
          resources:
            {{- toYaml .Values.ViewerApiResources | nindent 12 }}
          volumeMounts:
            - name: logdir
              mountPath: /opt/imaging/imaging-api/logs
              readOnly: false
            - name: api-config-dir
              mountPath: /opt/imaging/imaging-api/config
      volumes:
        - name: logdir
          persistentVolumeClaim:
            claimName: pvc-imagingviewer-v3-api-logs
        - name: api-config-dir
          emptyDir: {}
{{- if .Values.UseImagePullSecrets }}              
      imagePullSecrets:
        - name: imagepull-sec
{{- end }}  