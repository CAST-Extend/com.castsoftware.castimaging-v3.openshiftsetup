apiVersion: v1
kind: Service
metadata:
  labels:
    app: castimaging
    imaging.group: console
    imaging.service: console-authentication-service
  name: console-authentication-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: "8092"
      port: 8092
      targetPort: 8092
  selector:
    imaging.service: console-authentication-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: castimaging
    imaging.group: console
    imaging.service: console-authentication-service
    version: {{ .Values.ConsoleAuthenticationServiceImage.tag| default .Values.version }}
  namespace: {{ .Release.Namespace }}
  name: console-authentication-service
spec:
  replicas: {{ .Values.AuthenticationServiceReplicaCount }}
  selector:
    matchLabels:
      imaging.service: console-authentication-service
  strategy: {}
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
      labels:
        console.network/aip-network: "true"
        app: castimaging
        imaging.group: console
        imaging.service: console-authentication-service
    spec:
{{- if .Values.EnforceNodeAffinity }}
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.nodeSelectorAppcastimaging | nindent 12 }}
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorServiceSso | nindent 12 }}
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupConsole | nindent 12 }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.labelSelectorGroupViewer | nindent 12 }}
{{- end }}
{{- if .Values.EnforceBalancedAffinity }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            {{- toYaml .Values.podAffinityGroupViewer | nindent 12 }}
{{- end }}
      initContainers:
{{- if .Values.UseCustomTrustStore }}
        - name: init-folders
          image: "{{.Values.ConsoleAuthenticationServiceImage.name}}:{{.Values.ConsoleAuthenticationServiceImage.tag}}"
          imagePullPolicy: "{{.Values.ConsoleAuthenticationServiceImage.pullPolicy}}"
          command:
          - /bin/sh
          - -c
          - |
            rm -rf /cast-volume/*
            cp -r /cast/* /cast-volume/ || true
          volumeMounts:    
            - name: cast-dir
              mountPath: /cast-volume 
          resources:
            {{- toYaml .Values.InitContainerResources | nindent 12 }}
{{- end }}
        - name: console-sso-control-panel-healthcheck
          image: {{ .Values.InitUtilImage.name }}:{{ .Values.InitUtilImage.tag }}
          imagePullPolicy: "{{ .Values.InitUtilImage.pullPolicy }}"
          command:
          - /bin/sh
          - -c
          - |
            until curl -sf -o /dev/null -w "%{http_code}" "http://console-sso-service:8096/auth/" 2>/dev/null | grep -q "302\|200"; do
              echo "Waiting for Keycloak to be ready..."
              sleep 5
            done
            echo "Keycloak is ready!"

            until curl -s --fail --max-time 5 http://console-control-panel:8098/actuator/health | grep -q '"status":"UP"'; do
              echo "Waiting for console-control-panel to be healthy..."
              sleep 10
            done
            echo "console-control-panel is healthy!"
          resources:
            {{- toYaml .Values.InitContainerResources | nindent 12 }}
      containers:
        - env:
{{- range $key, $value := .Values.AUTHENTICATIONSERVICE }}
            - name: {{ $key }}
              value: {{ $value | quote }}
{{- end }}
{{- if .Values.UseCustomTrustStore }}
            - name: USE_CUSTOM_TRUSTSTORE
              value: "true"
            - name: TRUSTSTORE_PATH
              value: /cast/truststore.jks
            - name: TRUSTSTORE_PASSWORD
              value: castapi
{{- end }}                      
            - name: HOST_AUTH_SERVICE
              value: console-authentication-service
            - name: PORT_AUTH_SERVICE
              value: "8092"
            - name: HOST_CONTROL_PANEL
              value: console-control-panel
            - name: PORT_CONTROL_PANEL
              value: "8098"
            - name: HOST_KEYCLOAK
              value: console-sso-service
            - name: PORT_KEYCLOAK
              value: "8096"
{{- if .Values.CustomPostgres.enable }}
            - name: DB_HOST
              value: {{ .Values.CustomPostgres.host }}
            - name: DB_PORT
              value: "{{ .Values.CustomPostgres.port }}"               
{{- else }}
            - name: DB_HOST
              value: console-postgres
            - name: DB_PORT
              value: "2285"
{{- end }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default "imaging-pwd-sec" }}
                  key: operator-db-password
            - name: KC_USER
              value: "admin"
            - name: KC_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecret | default "imaging-pwd-sec" }}
                  key: keycloak-admin-password
            - name: HOST_GATEWAY
              value: console-gateway-service
            - name: PORT_GATEWAY
              value: "8090"
{{- if .Values.ContextUrl.enable }}
            - name: NGINX_HOST
              value: {{ .Values.FrontEndHost }}{{ .Values.ContextUrl.appcontext }}
            - name: APP_CONTEXT
              value: {{ .Values.ContextUrl.appcontext }}
{{- else }}
            - name: NGINX_HOST
              value: {{ .Values.FrontEndHost }}
{{- end }}
          image: "{{ .Values.ConsoleAuthenticationServiceImage.name }}:{{ .Values.ConsoleAuthenticationServiceImage.tag }}"
          imagePullPolicy: "{{ .Values.ConsoleAuthenticationServiceImage.pullPolicy }}"
          name: console-authentication-service
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8092
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8092
              scheme: HTTP
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8092
              scheme: HTTP
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 20
            successThreshold: 1
          ports:
            - containerPort: 8092
              protocol: TCP
          resources:
            {{- toYaml .Values.AuthenticationServiceResources | nindent 12 }}
{{- if .Values.UseCustomTrustStore }}
          volumeMounts:
            - name: cast-dir
              mountPath: /cast
            - name: authcacrt
              mountPath: /cast/certs/ca.crt
              subPath: ca.crt 
      volumes:
        - name: cast-dir
          emptyDir: {}
        - name: authcacrt
          configMap:
            name: authcacrt
            defaultMode: 0755          
{{- end }}
      restartPolicy: Always
{{- if .Values.UseImagePullSecrets }}      
      imagePullSecrets:
        - name: imagepull-sec
{{- end }}        